using VideoPlayer.Models;
using VideoPlayer.PageModels;

namespace VideoPlayer.Pages
{
    public partial class MainPage : ContentPage
    {
        public MainPage(MainPageModel model)
        {
            InitializeComponent();
            BindingContext = model;
        }
    }
}